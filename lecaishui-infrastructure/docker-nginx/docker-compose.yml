nginx:
  image: nginx
  volumes:
    - ./conf/nginx.conf:/etc/nginx/nginx.conf
  ports:
    - "80:80"
  restart: always
  volumes_from:
    - disconf-data
  links:
    - disconf-webapp:disconf-webapp